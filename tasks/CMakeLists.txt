# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(laser_filterTaskLib)
ADD_LIBRARY(${LASER_FILTER_TASKLIB_NAME} SHARED 
    ${LASER_FILTER_TASKLIB_SOURCES})

add_dependencies(${LASER_FILTER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${LASER_FILTER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${LASER_FILTER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${LASER_FILTER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LASER_FILTER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LASER_FILTER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LASER_FILTER_TASKLIB_HEADERS}
    DESTINATION include/orocos/laser_filter)

